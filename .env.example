# DeepSeek API Key 配置
#
# 本地开发：复制此文件为 .env.local，填入真实 key
#   cp .env.example .env.local
#   然后编辑 .env.local 填入 VITE_DEEPSEEK_API_KEY=sk-xxx
#
# 线上部署：在 GitHub 仓库 Settings → Secrets and variables → Actions
#   添加一个名为 VITE_DEEPSEEK_API_KEY 的 secret，值为真实 key。
#   CI 构建时会注入并由 Vite 静态写进产物，线上测试人员免填。
#
# ⚠️ 安全提醒：VITE_ 前缀变量在构建时会被写死进前端 JS，
#    线上产物源码可见。本项目为内部测试、key 后续会作废，故接受此风险。
#    切勿将填有真实 key 的 .env.local 提交到代码库（已被 .gitignore 忽略）。
VITE_DEEPSEEK_API_KEY=
